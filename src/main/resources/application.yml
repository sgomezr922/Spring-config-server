#APACHE
server:
  port: 8888

#CONFIG SERVER
spring:
  cloud:
    config:
     server:
      git:
        uri: https://github.com/sgomezr922/SpringConfig
  security:
    user:
      name: user
      password: password
#spring.cloud.config.server.git.basedir=